#include <stdio.h>
int main(void) {
  int num1, num2, sum;
  printf("the first number is\n");
  scanf("%d", &num1);
  printf("the second number is\n");
  scanf("%d", &num2);
  sum = num1 + num2;
  printf("sum is %d\n", sum);

  return 0;
}
